<Window x:Class="Typist.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Typist.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="Typist — Typing Tutorial" Height="760" Width="1200"
        MinHeight="600" MinWidth="900"
        Icon="AppIcon.png"
        Background="{StaticResource AppBackground}"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewTextInput="Window_PreviewTextInput"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- =================== SIDEBAR =================== -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBg}"
                BorderBrush="{StaticResource Border1}" BorderThickness="0,0,1,0">
            <DockPanel>

                <!-- Logo / App Name -->
                <StackPanel DockPanel.Dock="Top" Margin="20,28,20,32">
                    <TextBlock Text="&#x2328;" FontSize="28" Foreground="{StaticResource AccentCyan}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="Typist" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Typing Tutorial" FontSize="11"
                               Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <!-- Navigation Items -->
                <StackPanel DockPanel.Dock="Top" Margin="12,0">

                    <!-- Dashboard -->
                    <Button Margin="0,2" Command="{Binding NavigateToCommand}" CommandParameter="Dashboard">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentViewName}" Value="Dashboard">
                                        <Setter Property="Background" Value="{StaticResource SidebarActive}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F3E0;" FontSize="16" Width="28" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="Dashboard" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Lessons -->
                    <Button Margin="0,2" Command="{Binding NavigateToCommand}" CommandParameter="Lessons">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentViewName}" Value="Lessons">
                                        <Setter Property="Background" Value="{StaticResource SidebarActive}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F4DA;" FontSize="16" Width="28" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="Lessons" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Speed Test -->
                    <Button Margin="0,2" Command="{Binding NavigateToCommand}" CommandParameter="Test">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentViewName}" Value="Test">
                                        <Setter Property="Background" Value="{StaticResource SidebarActive}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x26A1;" FontSize="16" Width="28" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="Speed Test" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Statistics -->
                    <Button Margin="0,2" Command="{Binding NavigateToCommand}" CommandParameter="Statistics">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentViewName}" Value="Statistics">
                                        <Setter Property="Background" Value="{StaticResource SidebarActive}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F4CA;" FontSize="16" Width="28" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="Statistics" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                </StackPanel>

                <!-- Settings at bottom -->
                <StackPanel DockPanel.Dock="Bottom" Margin="12,0,12,20">
                    <Separator Background="{StaticResource Border1}" Margin="0,0,0,12"/>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Settings">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentViewName}" Value="Settings">
                                        <Setter Property="Background" Value="{StaticResource SidebarActive}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x2699;" FontSize="16" Width="28" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="Settings" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </DockPanel>
        </Border>

        <!-- =================== CONTENT AREA =================== -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>

    </Grid>
</Window>
